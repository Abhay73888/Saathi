# Vercel (and Render) run installs with NODE_ENV=production, which makes npm skip
# devDependencies. The web/api builds need that toolchain (typescript/tsc,
# tailwindcss, prisma CLI), so keep dev dependencies installed everywhere.
include=dev
engine-strict=false
fund=false
audit=false
